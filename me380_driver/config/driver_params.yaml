driver_node:
  ros__parameters:
    serial_port: "/dev/ttyUSB0"
    baud_rate: 9600
    joint_state_topic: "joint_states"
    max_stepper_delta_per_msg: 360.0

driver_node_py:
  ros__parameters:
    serial_port: "/dev/ttyUSB0"
    baud_rate: 9600
    joint_state_topic: "joint_states"
    max_stepper_delta_per_msg: 360.0
